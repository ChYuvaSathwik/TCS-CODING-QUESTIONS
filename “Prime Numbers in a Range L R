def range_left_right(L,R):
    sathwik=[]
    for i in range(L,R+1):
        M=True
        if i<2:
            continue
        else:
            for k in range(2,int(i**0.5)+1):
                if i%k==0:
                    M=False
                    break
            if M:
                sathwik.append(i)
    return sathwik
L,R=map(int,input().split())
print(range_left_right(L,R))

