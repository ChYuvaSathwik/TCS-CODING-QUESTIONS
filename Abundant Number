def abundant(N):
    count1=0
    for i in range(1,N):
        if N%i==0:
            count1=count1+i
    if count1>N:
        return "Abundant Number"
    else:
        return "Not Abundant Number"
N=int(input())
print(abundant(N))
