class Solution:
    def missingNum(self, arr):
        n = len(arr) + 1
        total_sum = n * (n + 1) // 2
        return total_sum - sum(arr)


arr = [1, 2, 3, 5]
sol = Solution()
print(sol.missingNum(arr))  
