def row_max(R,C):
    sathwik=[]
    h=float("-inf")
    for i in range(R):
        M=list(map(int,input().split()))
        sathwik.append(M)
    for k in sathwik:
        for s in k:
            h=max(h,s)
    return h
R,C=map(int,input().split())
print(row_max(R,C))
