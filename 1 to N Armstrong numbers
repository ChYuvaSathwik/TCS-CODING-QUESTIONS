def all_armstrong_number(N):
    Balu = []
    for i in range(1, N+1):
        M = str(i)
        sathwik = len(M)
        count1 = 0
        for h in M:
            count1 += int(h) ** sathwik
        if count1 == i:
            Balu.append(count1)
    return Balu

N = int(input())
print(all_armstrong_number(N))
