def target_subarray(N,target):
    for i in range(len(N)):
        for j in range(i+1,len(N)+1):
            if sum(N[i:j])==target:
                return N[i:j]
N=list(map(int,input().split()))
target=int(input())
print(target_subarray(N,target))
